replicaCount: 2
image:
  repository: registry.gitlab.com/example/frontend
  tag: latest
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  port: 8080
serviceAccount:
  create: true
  name: frontend
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
hpa:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 60
ingress:
  enabled: true
  className: nginx
  host: app.lab.local
  tlsSecretName: frontend-tls
config:
  ENVIRONMENT: dev
vault:
  enabled: true
  role: frontend
  secretPath: secret/data/app/frontend
