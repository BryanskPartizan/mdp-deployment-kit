{{- if .Values.vault.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-vault-placeholder
  annotations:
    vault.example.io/path: secret/data/app/{{ .Chart.Name }}
type: Opaque
stringData:
  PLACEHOLDER: managed-by-vault-flow
{{- end }}
