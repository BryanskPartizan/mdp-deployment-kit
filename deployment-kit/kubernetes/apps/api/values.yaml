replicaCount: 2
image:
  repository: registry.pkhco.ru/platform/api
  tag: "0.2.0"
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  port: 8081
serviceAccount:
  create: true
  name: api
  # Vault Agent использует ServiceAccount JWT для Kubernetes auth; отключать токен можно только после смены auth-модели.
  automountToken: true
podSecurityContext:
  runAsNonRoot: true
  seccompProfile:
    type: RuntimeDefault
containerSecurityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
hpa:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 60
pdb:
  enabled: true
  minAvailable: 1
topologySpread:
  enabled: true
  maxSkew: 1
  topologyKey: kubernetes.io/hostname
  whenUnsatisfiable: ScheduleAnyway
networkPolicy:
  enabled: true
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security
      ports:
        - protocol: TCP
          port: 8200
    - to:
        - podSelector:
            matchExpressions:
              - key: app.kubernetes.io/name
                operator: In
                values:
                  - postgres
                  - postgresql
              - key: app.kubernetes.io/instance
                operator: In
                values:
                  - postgres
      ports:
        - protocol: TCP
          port: 5432
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: redis
              app.kubernetes.io/instance: redis
      ports:
        - protocol: TCP
          port: 6379
ingress:
  enabled: false
  className: nginx
  clusterIssuer: letsencrypt-prod
  host: api.pkhco.ru
  tlsSecretName: api-tls
config:
  ENVIRONMENT: dev
vault:
  enabled: true
  role: api
  secretPath: secret/data/app/api
