# Совместимость со старым путём include. Jobs приложений перенесены в общий infra pipeline,
# потому что GitLab разворачивается тем же deployment-kit и не является внешней предпосылкой.
include:
  - local: ci/templates/infra-pipeline.yml
